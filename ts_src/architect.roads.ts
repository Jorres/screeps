// @ts-ignore
var data = require('data');
// @ts-ignore
var U = require('U');

var architectRoads = {
    run: function(spawn: StructureSpawn): void {
        console.log("containers roads running...");
    }
};

// @ts-ignore
module.exports = architectRoads;
