var data = require('data');
var config = require('config');
var U = require('U');
var architectContainers = {
    run: function () {
        console.log("architector running...");
    }
};
module.exports = architectContainers;
